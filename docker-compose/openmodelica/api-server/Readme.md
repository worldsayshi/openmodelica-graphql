A simple server to allow using compiling and starting interactive modelica models.

**Not** intended to be exposed directly to the internet.
But as backend for another server that knows what it's doing.

# How

```
$ nvm use
$ npm i
@ npm start
```